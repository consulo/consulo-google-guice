#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")package ${PACKAGE_NAME};#end
import com.google.inject.Provider;

#parse("Java File Header.java")
public class ${NAME} implements Provider<${PROVIDER_CLASS_NAME}> {
	public ${PROVIDER_CLASS_NAME} get() {
		//TODO: add provider logic here
		return null;
	}
}